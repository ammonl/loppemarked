// `@un17/logo` ships a plain JS custom-element bundle with no type
// declarations. It's imported only for its registration side effect, so an
// empty module declaration is all TypeScript needs.
declare module "@un17/logo";
